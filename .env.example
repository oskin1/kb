# Copy this to .env and fill in your API key(s)
# Only needed for entity/fact extraction (Phases 2-5)
# Basic ingest + search works without any API key.

# OpenAI (if llm.provider = openai in config.yaml)
OPENAI_API_KEY=sk-...

# Anthropic (if llm.provider = anthropic in config.yaml)
# ANTHROPIC_API_KEY=sk-ant-...
